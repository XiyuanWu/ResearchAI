# Copy to .env and fill in values. Do not commit .env.

DJANGO_SECRET_KEY=change-me-in-local-dev
DJANGO_DEBUG=True

# Add at least one key before connecting a real LLM (step 4+)
OPENAI_API_KEY=
GEMINI_API_KEY=
